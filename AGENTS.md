# Agent Instructions

## Versioning

Every time you commit and push, increment the patch version by 1 in `personal_cycling_agent/config.yaml`:

```yaml
version: "X.Y.Z"  # bump Z by 1 on each commit
```

Example: `0.0.1` → `0.0.2` → `0.0.3`